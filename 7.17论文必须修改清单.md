# 《PFDNet》论文必须修改清单

审阅对象：`/Volumes/Can Disk/writing/review/7.17.pdf`（13 页）  
审阅范围：语法、术语、公式、论证逻辑、实验协议、图表与可复现性。  

> 说明：下面仅列建议在投稿前实际修改的内容。A 类属于会影响正确性、实验可信度或复现性的硬问题；B 类属于明确的语法、符号或前后不一致问题。建议先完成 A 类，再统一处理 B 类。

## A. 优先级最高：正确性、逻辑与可复现性

### 第 III 章：PFDNet Methodology

#### A1. 式 (6) 的 AoLP 计算存在象限歧义（第 4 页，III-B）

- **原文/公式**：`AoLP = 1/2 arctan((I45-I135)/(I0-I90))`。
- **什么不对**：普通 `arctan(y/x)` 无法区分象限，且当 `I0-I90 = 0` 时不稳定。这样计算出的 AoLP 可能相差 90°，与后文强调的 π 周期性也不完全一致。
- **为什么必须改**：这是偏振量定义层面的正确性问题，会影响先验输入、输出真值以及表 II 的 AoLP 评价。
- **建议改成**：

  ```text
  AoLP = 1/2 atan2(I45-I135, I0-I90),  AoLP ∈ [0, π)
  ```

  并明确负值如何映射到 `[0, π)`。

#### A2. 论文没有给出任何训练目标/损失函数（第 4–6 页，III 章）

- **原文情况**：方法章从编码器、PWFFM、PCTI 直接进入实验，没有定义总损失；实验只说网络为每个层预测五个通道。
- **什么不对**：读者不知道 transmission/reflection、四个偏振角、总强度、DoLP/AoLP 分别如何监督，也不知道各损失的权重、物理一致性约束和输出范围。
- **为什么必须改**：模型无法复现；同时无法判断表 I、表 II 的提升来自结构还是损失设计。
- **建议改成**：在 III 章末新增 **Training Objectives**，至少定义：

  1. transmission 与 reflection 的重建损失；
  2. 四偏振角通道的监督方式；
  3. 若监督 DoLP/AoLP，AoLP 应使用周期感知损失，例如 `1-cos(2(θ_pred-θ_gt))`；
  4. 混合重建一致性（如适用）；
  5. 总损失及每个权重；
  6. 输出激活、归一化范围和有效区域 mask。

#### A3. 输出“五通道”与式 (4) 的物理关系冲突，且符号 `I`/`S0` 不一致（第 4、7 页，III-B、IV-A）

- **原文**：式 (4) 将总强度定义为 `S0`；Implementation Details 又写输入包含四个角度和 total intensity channel `I`，每个恢复层预测 `I0, I45, I90, I135, I` 五个通道。
- **什么不对**：总强度由四个角度可计算，单独预测会产生冗余和物理不一致；`I`、`S0`、图中的 `T/R` 又没有说明是单通道总强度还是五通道张量。
- **为什么必须改**：输入输出定义是复现模型的基础，也直接影响 DoLP/AoLP 的生成方式。
- **建议改成**：二选一并全文统一。

  - 推荐：仅预测四个偏振角通道，再按式 (4) 计算 `S0`；或
  - 保留第五通道，但增加并定义 `S0` 一致性损失，说明该通道的用途。

  同时统一使用 `S0`，明确 `T`、`R` 是总强度图还是偏振图像组。

#### A4. PWFFM 中多组符号和运算未定义（第 5 页，III-D，式 (15)–(18)）

- **原文情况**：`P_b`、`g_A^s`、`g_Θ^s`、`u_s^q`、`G_s,q`、`δ`、`ω_L`、`Re` 没有完整定义；`[·]` 是否表示 concat 也未说明；相位更新单位/范围不清楚。
- **什么不对**：正文只描述功能，不能从公式唯一还原模块。图 3 中的具体卷积路径也不能替代数学定义。
- **为什么必须改**：核心创新模块不可复现，而且相位直接相加时必须说明表示、范围与 wrap 处理。
- **建议改成**：逐一给出符号定义、张量尺寸、卷积核/激活函数和 concat 维度；明确 `P_b` 如何由 `P_prior` 得到；说明相位是否在 `[-π,π)`、如何处理环绕；定义 `u_s^q(·)` 和 gate 的生成网络。若 `δ`、`ω_L` 只是激活和卷积，应直接写明。

#### A5. 图 2 中的 PSRM 在正文中没有定义（第 3–5 页，III-B/III-C）

- **原文/图示**：图 2 和图例出现 **Polarization-guided Skip Refinement Module (PSRM)**，正文只笼统写 “polarization-refined skip connections”，没有结构、公式或实现说明。
- **什么不对**：PSRM 位于多级 skip connection，是完整网络的一部分，却未进入方法描述或消融实验。
- **为什么必须改**：网络结构不可复现，也无法判断性能贡献。
- **建议改成**：新增 PSRM 小节，说明每一级输入、输出、参数是否共享、具体操作和张量尺寸；若它并非独立模块，则从图和图例删除 PSRM，并把实际 skip 操作写清楚。最好增加对应消融，或解释为何不单独消融。

#### A6. PCTI 的注意力维度与缩放项说明不足（第 6 页，III-E，式 (19)）

- **原文**：`Softmax(Q_s K^T_sbar / sqrt(C)) V_sbar`。
- **什么不对**：没有给出 `Q/K/V` 的 reshape 方式、注意力沿哪个维度归一化、`C` 是否就是每个 head 的维度，也没有说明是否为多头注意力。图 4 标的是 `sqrt(d)`，正文却是 `sqrt(C)`。
- **为什么必须改**：图文矛盾，且计算形式无法复现。
- **建议改成**：统一图 4 和式 (19)；明确 `Q,K,V ∈ R^{...}`、token 数、channel/head dimension、softmax 轴。如果没有多头，明确 `d=C`；如果是多头，应使用并定义 `d_h`。

#### A7. 式 (21) 的变量重名，且段落标点错误（第 6 页，III-E）

- **原文**：将 recalibrated feature 命名为 `P_s`，而全文 `P` 已用于 polarization prior；解释句以冒号结束，下一段却开始 “Finally”。
- **什么不对**：`P_s` 容易被理解为新的偏振先验，造成公式 (21)–(22) 语义混乱。
- **建议改成**：把式 (21) 输出改为不冲突的名称，如 `Z_s`；将 “... polarization feature Pc:” 末尾冒号改为句号，并相应更新式 (22)。

#### A8. 双反射模型与真实训练数据之间缺少对应说明（第 3、6–7 页，III-A、IV-A）

- **原文**：式 (1) 假设 `I=T+Rg`，其中 ghosting 由两脉冲 kernel 描述；数据则通过“放黑布”和“移除玻璃”分别拍摄 reflection/transmission。
- **什么不对**：移除玻璃后拍到的 transmission 未必等于混合图中的透射项（玻璃会引入透射率、色偏、偏振变化、折射/几何位移）；实拍 reflection 也未说明是否满足两脉冲模型。
- **为什么必须改**：监督真值与成像模型是否一致会影响所有 quantitative results 的有效性。
- **建议改成**：给出更完整的成像式，例如包含透射/反射系数和相机响应；说明 fixed exposure、白平衡、线性 RAW、配准及辐射校准方法。若式 (1) 只用于动机而不是精确数据模型，应明确写成 “a simplified motivational model”，避免暗示数据严格满足该式。

### 第 IV 章：Experiments

#### A9. PHR 数据集真值采集描述不足（第 6–7 页，IV-A）

- **原文**：先拍 mixed，放置黑色吸光布拍 reflection，移除玻璃拍 clean transmission，并称三者 aligned。
- **什么不对**：没有说明每一步是否都拍摄四个偏振角；移动黑布和移除玻璃如何保持场景、相机、曝光完全一致；也没有说明配准误差、线性化、环境光稳定性和 `mixed ≈ transmission + reflection` 的验证。
- **为什么必须改**：这是新数据集与主要实验的根基。
- **建议改成**：补充完整采集协议：相机/镜头/偏振器型号，偏振角控制方式，RAW/曝光设置，玻璃和场景固定方式，每个真值包含哪些通道，配准流程及误差，辐射校准，以及混合重建残差统计。最好给出 `||I-(T+R)||` 的定量分布。

#### A10. “80% reflectivity” 缺乏定义与测量依据（第 6 页，IV-A）

- **原文**：`All samples are captured through a glass panel with 80% reflectivity.`
- **什么不对**：未说明这是厂商标称、实测还是由图像比值估算；也未说明波段、入射角、偏振态及玻璃是否带反射涂层。普通 “glass panel” 的表述会使该数值显得异常且不可验证。
- **建议改成**：说明玻璃类型（如 coated glass/beam splitter）、测量方法、波长范围和入射角；若只是标称值，写成 `nominal reflectance of 80%`，并报告实拍场景中的 `R/T` 统计作为实际难度指标。

#### A11. `R/T` 指标未定义（第 6–7 页，图 5(b)、IV-A）

- **原文**：`mean intensity ratio R/T`。
- **什么不对**：不清楚是 `mean(R)/mean(T)`、`mean(R/T)`，还是对每张图先求比值再统计；也没有说明颜色通道、有效 mask、归一化和箱线图样本单位。
- **为什么必须改**：不同定义会得到明显不同的结论，图 5(b) 目前不可复现。
- **建议改成**：给出明确公式、预处理、样本数和箱线图含义，并说明不同数据集的曝光/强度标度是否可直接比较。

#### A12. 数据划分可能存在场景泄漏，需说明 scene-disjoint 原则（第 6 页，IV-A）

- **原文**：500 image sets，按 400/50/50 划分。
- **什么不对**：没有说明同一场景、同一 transmission/reflection 组合或相邻拍摄是否会跨 train/validation/test。
- **为什么必须改**：如果近重复场景跨集合，结果会被高估。
- **建议改成**：说明划分单位和唯一场景数量，保证 scene-disjoint；若当前不是 scene-disjoint，应重新划分并重跑实验。

#### A13. “cross-dataset generalization” 没有对应训练/测试协议（第 6、8 页，IV-A/IV-B）

- **原文**：声称三个数据集用于评估 cross-dataset generalization，但表 I 只给出各数据集结果，没有说明模型在哪个数据集训练、是否分别微调。
- **什么不对**：如果每个数据集分别训练，这不是跨数据集泛化；如果只在一个数据集训练，也必须明确源域和目标域。
- **建议改成**：为每列标明训练集/测试集和是否 fine-tune。若没有真正的跨数据集实验，删除该 claim；若要保留，应增加 “train on A, test directly on B/C” 的独立表格。

#### A14. 基线训练与适配协议缺失，表 I 的公平性无法判断（第 7–8 页，IV-B、表 I）

- **原文**：只写非偏振方法使用 total intensity，偏振方法使用 full polarized observation。
- **什么不对**：没有说明使用作者权重还是重训、训练数据和 crop/epoch 是否一致、五通道输入如何适配、缺失 reflection output 的方法如何处理，以及各方法是否接触 PHR 训练集。
- **为什么必须改**：这是 SOTA comparison 的必要信息。
- **建议改成**：新增 **Baseline implementation protocol**，逐项说明 checkpoint、官方代码版本、输入适配、重训/微调数据、超参数和输出生成方式。必要时放入补充材料，但正文需有摘要。

#### A15. 表 I 的 `w/ Polar` 标记与引用方法性质疑似矛盾（第 6–8 页，表 I、IV-B）

- **原文**：Reti-Diff [35] 被标为使用 polarization；但 [35] 的题名是 illumination degradation restoration，并非偏振反射去除方法。ReflectNet 等方法的输入适配也未解释。
- **什么不对**：读者无法判断勾号表示“原方法原生支持偏振”还是“本文把它改成偏振输入”。
- **建议改成**：把列名拆成 `Native polarized input` 与 `Adapted input in our experiment`，或在表注逐方法解释。如果 Reti-Diff 仅使用 total intensity，应更正为叉号；若做了多通道改造，应说明结构改动和训练方式。

#### A16. Schechner [24] 与任务/方法名可能引用不匹配（第 6–8 页，表 I、IV-B）

- **原文**：以 `Schechner [24]` 作为 reflection-removal baseline；[24] 实际题名为 **Active polarization descattering**。
- **什么不对**：descattering 并不等同于玻璃反射分离；若使用的是另一篇 Schechner 反射分离方法，则当前引用错误。
- **建议改成**：核对实际代码/算法来源。若确实将 descattering 方法改用于本任务，要说明适配方式与合理性；否则替换为正确的反射分离引用。

#### A17. 表 II 的方法选择不完整且缺乏解释（第 8–9 页，表 II）

- **原文**：表 I 有 RP2PN、PolarFree、Reti-Diff 等，表 II 只比较 Schechner、Farid、ReflectNet、Lyu、Lei 和 PFDNet。
- **什么不对**：至少 RP2PN/PolarFree 属于偏振相关方法，却未出现在 polarization recovery comparison；未说明它们不能输出四角图/DoLP/AoLP，容易造成选择性比较的质疑。
- **建议改成**：加入所有能够产生偏振输出的相关方法；不能加入的，在表注或正文逐一说明技术原因。对只输出 transmission intensity 的方法，不应默认为无法评价，需说明是否可从其输出构造偏振量。

#### A18. AoLP 直接使用普通 PSNR/SSIM 不适合周期变量（第 8–9 页，IV-B、表 II）

- **原文**：对 AoLP map 报告 PSNR 和 SSIM，未定义角度误差或 wrap 处理。
- **什么不对**：AoLP 以 π 为周期。例如 1° 与 179° 物理上只差 2°，但在普通灰度 PSNR 中会被视为接近最大误差。
- **为什么必须改**：表 II 的 AoLP 数值可能不具有正确物理意义。
- **建议改成**：优先增加周期感知指标，如

  ```text
  Δθ = 0.5 * |atan2(sin(2(θp-θg)), cos(2(θp-θg)))|
  ```

  并报告 mean/median angular error。若保留 PSNR/SSIM，应在 `sin(2AoLP), cos(2AoLP)` 表示上计算并清楚说明，不能直接对原始角度灰度图计算。

#### A19. DoLP/AoLP 评价缺少有效区域 mask（第 4、8–9 页，式 (8)、表 II）

- **原文**：方法中定义了暗区/饱和区 mask `M`，实验中却没有说明表 II 是否应用该 mask；AoLP 在低 DoLP 区域本身也不可靠。
- **什么不对**：暗区、饱和区及近非偏振区域会产生不稳定角度，可能主导评价结果。
- **建议改成**：明确评价 mask，包括强度上下限和最小 DoLP 阈值；报告 mask 覆盖率；所有方法使用完全相同的 GT mask。可同时报告全图结果作为补充。

#### A20. 表 I/II 的统计显著性和重复性不足（第 6、9 页）

- **原文**：只报告单个 PSNR/SSIM/LPIPS 数值；训练只说随机种子固定为 2026。
- **什么不对**：若提升较小（如表 I 中 PHR SSIM 0.937 vs 0.938），单次训练无法判断差异是否稳定；“best” 的表述也需要谨慎。
- **建议改成**：至少对自有模型进行 3 次独立训练并报告 mean±std，或说明计算资源限制并提供 bootstrap confidence interval。表中并列/次优值应按实际数值正确标注。

#### A21. 没有报告计算量和运行效率，却在结论中把效率作为未来工作（第 7、12 页，IV-A、VI）

- **原文**：只报告参数量（表 IV）和 GPU 型号；未来工作称要降低 computational complexity。
- **什么不对**：读者无法判断当前 72.54M 参数模型的实际成本，也无法公平比较频域模块。
- **建议改成**：报告固定分辨率下的 FLOPs/MACs、推理时间、峰值显存、训练时间，并与主要基线比较。若版面有限，至少在补充材料提供。

### 第 V 章：Applications of Restored Polarization Information

#### A22. 图 11(a) 的 “Training strategy” 与正文的 inference 描述矛盾（第 11–12 页，V、图 11）

- **原文**：正文说将两种输入送入同一个 SfP-Wild，参数和 inference settings 不变；图中却标 `(a) Training strategy`。
- **什么不对**：一个表述是固定模型推理，另一个暗示重新训练。
- **建议改成**：如果未训练，图中改为 `(a) Evaluation pipeline` 或 `(a) Inference pipeline`；如果确实训练过，则补充训练集、loss、epoch 和两组是否使用相同训练协议。

#### A23. Surface normal “improvement” 只有可视化，没有定量验证（第 11 页，V）

- **原文**：声称 normal map “better preserves surface orientation with fewer artifacts”。
- **什么不对**：没有 normal GT、angular error 或用户研究，结论仅来自主观观察。
- **建议改成**：有 GT 时报告 mean/median angular error 和阈值准确率；没有 GT 时把措辞降为 `qualitatively appears more coherent`，并明确这是 qualitative demonstration，不要写成已被验证的性能提升。

#### A24. Real-world vehicle-glass 图中的 “Transmission” 来源不明（第 11–12 页，V、图 11(b)）

- **原文/图示**：列名为 Mixed、Transmission、Ours，但正文只称 real-world scenes，没有说明 Transmission 是 GT、参考曝光还是另一张非对齐图。
- **什么不对**：若是真值，需要采集协议；若不是严格真值，标签会误导。
- **建议改成**：正文和图注明确该列来源。严格配准真值写 `Ground-truth transmission` 并描述采集；非配准参考写 `Reference (unregistered)`，且不要用于像素级评价。

#### A25. “Material distinction” 的结论超出当前证据（第 11–12 页，V）

- **原文**：把 DoLP 可视化称为 material distinction，并声称不同材料呈现不同分布。
- **什么不对**：没有分类器、标注、定量指标或统计检验；DoLP 同时受几何、入射角、粗糙度和折射率影响，不能仅凭图直接证明材料可区分。
- **建议改成**：二选一：

  - 增加材料标注、分类/分割协议和 quantitative results；或
  - 将小节改成 `Qualitative visualization of material-related polarization responses`，并把结论限定为“显示潜在用途”，同时说明几何等混杂因素。

### 第 VI 章：Conclusion

#### A26. 结论中的性能措辞与实验陈述不一致（第 12 页，VI）

- **原文**：`achieves comparable results on most quantitative metrics`；摘要和实验则称 best/improved。
- **什么不对**：`comparable` 通常表示相近而非更优，与表 I/II 的主张不一致；同时表 I 又确有少数指标不是第一，不能简单写全方面 SOTA。
- **建议改成**：使用精确而克制的表述，例如：

  ```text
  PFDNet achieves the best results on most evaluated metrics, while remaining competitive on the others.
  ```

  若补充多次运行后差异无统计显著性，应改为 `competitive` 而不是 `best`。

## B. 明确的语法、符号、图表与一致性问题

### 题目、作者信息与首页脚注（第 1 页）

#### B1. 通讯作者邮箱含空格

- **原文**：`haofeng hu@tju.edu.cn`
- **问题**：邮箱无效。
- **建议改成**：`haofenghu@tju.edu.cn`（请与作者本人确认准确拼写）。

#### B2. 作者单位句缺少连词

- **原文**：`Lijun Deng, Yang Xu are with ...`；`Hedong Liu, Zixian Liu, Xiaobo Li, Haofeng Hu are with ...`
- **建议改成**：`Lijun Deng and Yang Xu are with ...`；`Hedong Liu, Zixian Liu, Xiaobo Li, and Haofeng Hu are with ...`。

#### B3. 致谢项目名称语法不通

- **原文**：`the Jiangxi Province the Voyage project (2025)`
- **问题**：重复冠词且项目名大小写/官方名称不清。
- **建议改成**：按官方英文名称核对；若名称确为 Voyage Project，可写 `the Jiangxi Province “Voyage” Project (2025)`。整句建议改为：

  ```text
  Lijun Deng gratefully acknowledges support from the China Scholarship Council (CSC) and the Jiangxi Province “Voyage” Project (2025) for his research visit to Nanyang Technological University, Singapore.
  ```

### 摘要（第 1 页）

#### B4. 复合形容词缺少连字符

- **原文**：`in high reflection scenarios`、`low frequency Fourier amplitude`、`large scale reflection disturbances`、`high frequency subbands`。
- **建议改成**：`in high-reflection scenarios`、`low-frequency Fourier amplitude`、`large-scale reflection disturbances`、`high-frequency subbands`。

#### B5. `integrates ... amplitude and ... phase` 的动词不准确

- **原文**：`PWFFM integrates low-frequency Fourier amplitude and Fourier phase to suppress ...`
- **问题**：后文实际是 modulation/modeling，不只是把二者“整合”；同时缺少冠词/并列层次。
- **建议改成**：`PWFFM jointly models low-frequency Fourier amplitude and phase to suppress large-scale reflection disturbances and recover attenuated transmission structures.`

#### B6. 摘要中的 “material distinction” 需与 A25 同步降级或补实验

- **原文**：`benefiting downstream applications such as ... material distinction.`
- **建议改成**：若不补定量实验，改为 `showing potential for ... material-related analysis`。

### 第 I 章：Introduction（第 1–2 页）

#### B7. `geometry related information` 缺连字符

- **建议改成**：`geometry-related information`。

#### B8. `polarization derived physical priors` 缺连字符

- **建议改成**：`polarization-derived physical priors`。

#### B9. `reflection related features` 缺连字符

- **建议改成**：`reflection-related features`。

#### B10. “amplitude mainly affects...” 的因果措辞过强

- **原文**：`Fourier amplitude mainly affects spectral energy distribution, whereas Fourier phase strongly influences spatial localization...`
- **问题**：图 1 的交换实验只能提供示意性证据，而且 amplitude/phase 都共同决定图像。
- **建议改成**：`Fourier amplitude primarily represents spectral energy, whereas phase carries important spatial-structure and localization information.` 并避免用单个示例证明普遍因果。

#### B11. 图 1(b) 的实验定义缺失

- **原文/图注**：`radially averaged reflection interference ratios`，正文直接据此得出跨频率结论。
- **问题**：没有公式、样本数、平均方式和 low/high reflection 分组标准。
- **建议改成**：在图注或实验设置中定义 interference ratio、径向频率归一化、样本数量、分组阈值及均值/方差。否则将该图定位为 illustrative example，并弱化结论。

#### B12. Contribution 3 的数据集命名不统一

- **原文**：`a polarized high-reflection dataset`；实验中为 `Polarized High-Reflection Dataset (PHR Dataset)`。
- **建议改成**：首次出现即使用正式全名和缩写，后文统一为 `PHR Dataset`。

### 第 II 章：Related Work（第 2–3 页）

#### B13. 小节标题与正文中的连字符需统一

- **原文**：`Frequency-Aware`、`frequency-aware`、`frequency domain` 等混用。
- **建议改成**：名词用 `frequency domain`，作前置形容词用 `frequency-domain` 或按全文选定 `frequency-aware`；不要同一含义交替写法。

#### B14. “diffusion-based reflection-free cues” 的引用对应关系需写清

- **原文**：`recurrent ..., diffusion-based ..., and attention-guided ... [10], [11], [26]–[28]`。
- **问题**：多个方法与多个类别合并引用，读者无法知道每篇属于哪类。
- **建议改成**：按类别分别附引用，例如 recurrent `[10]`、reflection-free/diffusion `[11], [28]`、attention `[26], [27]`，并核对 [11] 是否确属 diffusion。

### 第 III 章：PFDNet Methodology（第 3–6 页）

#### B15. 图 2 图例拼写错误

- **原文/图中**：`Concatentation`。
- **建议改成**：`Concatenation`。

#### B16. 图 2 图注复合形容词错误

- **原文**：`polarization enhanced representation`、`two parameter independent streams`。
- **建议改成**：`polarization-enhanced representation`、`two parameter-independent streams`。

#### B17. `Given four ... the total intensity is given:` 句法不完整

- **原文**：`Given four polarization-angle observations ..., the total intensity is given:`
- **建议改成**：`Given four polarization-angle observations ..., the total intensity is given by`，随后接式 (4)。

#### B18. 式 (8) 的指示函数写法需规范

- **原文**：`M = I(τ_l < max I_θ < τ_h)`，其中 `I` 同时又可能表示图像。
- **问题**：`I` 容易与 intensity 混淆，且没有定义 indicator function。
- **建议改成**：使用 `\mathbb{1}[·]` 并写明是逐像素 mask；同时解释为何用四通道最大值同时判断暗区和饱和区。

#### B19. AoLP double-angle 编码的范围需说明

- **原文**：只说跨 π-periodic boundary 连续。
- **建议改成**：补充 AoLP 映射范围 `[0,π)`，说明 `sin(2AoLP), cos(2AoLP)` 的数值范围及归一化方式。

#### B20. `branch specific`、`low frequency`、`high frequency` 等需统一加连字符

- **示例**：`branch specific RMFB`、`low frequency subband`、`high frequency path`、`direction aware gates`、`displacement related`。
- **建议改成**：`branch-specific`、`low-frequency`、`high-frequency`、`direction-aware`、`displacement-related`。全文批量检查这些作前置形容词的用法。

#### B21. 图 4 图注主谓不一致

- **原文**：`Cross-branch transformer interaction, adaptive cross-gate fusion, and polarization context enhancement recalibrates channel responses.`
- **建议改成**：`... recalibrate channel responses.`

#### B22. 图 4 与正文模块名称大小写应统一

- **原文**：`Polarization-guided Cross-branch Transformer Interaction` 中 Cross-branch 有时大写、有时小写；`Global Average Pooling` 在普通句中不必全部大写。
- **建议改成**：标题/模块正式名统一一种 capitalization；普通术语写 `global average pooling`。

### 第 IV 章：Experiments（第 6–11 页）

#### B23. 数据集名称存在三种写法

- **原文**：`Polarrr Dataset`、`Polar rr Dataset`、`Polar rr`；`Polarfree`、`PolarFree`；`Polarized High-Reflection Dataset`、`PHR Dataset`。
- **建议改成**：核对官方名称后全文统一，尤其是表 I、表 II、图 5 和正文。建议使用 `Polarrr Dataset`、`PolarFree Dataset`、`PHR Dataset`（若这正是官方拼写）。

#### B24. `high reflection glass imaging` 等复合形容词错误

- **原文**：`high reflection glass imaging`、`high reflection scenarios`、`reflection dominance analysis`。
- **建议改成**：`high-reflection glass imaging`、`high-reflection scenarios`、`reflection-dominance analysis`。

#### B25. Implementation Details 中 `I` 与 `S0` 必须统一

- **原文**：`the total intensity channel I`。
- **建议改成**：若指式 (4)，写 `the total-intensity channel S0`；并说明该通道是计算得到还是归一化后输入。

#### B26. `State-of-the-art` 标题大小写不符合常规

- **原文标题**：`Comparisons with State-of-the-art Methods`。
- **建议改成**：`Comparison with State-of-the-Art Methods`。

#### B27. 表 I 中缺失值必须加表注

- **原文**：PolarFree、Reti-Diff 在 reflection 指标处为 `–`，但没有解释。
- **建议改成**：表注说明缺失原因，例如该数据集无 reflection GT、方法不输出 reflection，或指标不可用；不同原因不能都只用同一个横线而不解释。

#### B28. 表 I/II 的 best/second-best 标注需逐项核对

- **例子**：PHR transmission SSIM 中 Reti-Diff 为 0.938，高于 PFDNet 的 0.937；Polarrr transmission LPIPS 中 RP2PN 与 PFDNet 均为 0.030。
- **建议改成**：明确 bold/underline 的含义，正确处理并列最优，避免把并非最优的数值统一加粗。

#### B29. Ablation variant 名称大小写和冠词不一致

- **原文**：`w/o Reflection Stream`、正文 `removing Reflection Stream`。
- **建议改成**：`w/o reflection stream`、`removing the reflection stream`；其余 variant 也采用统一句式。

#### B30. 表 IV 中 LPIPS 与文字论证需要更谨慎

- **原文**：Spatial-only 的 LPIPS 0.086 明显优于 full model 0.103，但正文主要强调 full model best。
- **问题**：虽然后文有解释，但表题/总结仍容易让人误读为 full model 所有指标最好。
- **建议改成**：明确写 `The full model achieves the best PSNR and SSIM, whereas Spatial-only obtains the lowest LPIPS.` 再说明层分离 fidelity 与感知指标之间的差异；不要笼统写 best performance。

#### B31. “3D error maps” 实际是柱状图，名称需更准确

- **原文/图 10**：`The 3D error maps show block-wise average absolute pixel errors.`
- **建议改成**：`The 3D bar plots visualize block-wise mean absolute errors.` 并给出 block size、误差归一化和色条单位。

### 第 V 章：Applications（第 11–12 页）

#### B32. SfP-Wild 大小写不统一

- **原文**：正文 `SfP-Wild`，图 11/图注出现 `Sfp-Wild`。
- **建议改成**：全文统一为论文/代码官方写法 `SfP-Wild`。

#### B33. 第 V 章任务列表存在类别混杂

- **原文**：称三个 downstream polarization-based perception tasks，包括 `real-world vehicle-glass reflection removal`。
- **问题**：反射去除是 PFDNet 本身的主任务/泛化测试，不是恢复偏振信息的 downstream perception task。
- **建议改成**：将 vehicle-glass 部分移到 IV 章的 real-world generalization；V 章只保留真正消费恢复后偏振量的 SfP 和 material-related analysis。或者将章名改为更宽泛的 `Applications and Real-World Generalization`。

### 第 VI 章：Conclusion（第 12 页）

#### B34. 首句堆叠多个 `reflection`，语义生硬

- **原文**：`a polarization-guided frequency-decoupling network for high reflection polarized reflection separation...`
- **建议改成**：

  ```text
  In this paper, we proposed PFDNet, a polarization-guided frequency-decoupling network for reflection separation and transmission-polarization recovery in high-reflection scenes.
  ```

#### B35. `material aware` 缺连字符

- **建议改成**：`material-aware image restoration`。

### References（第 12–13 页）

#### B36. 多条参考文献信息不完整

- **明显需要核对的条目**：
  - [10] 缺 volume/issue/pages 或 article number；
  - [25] ECCV 条目缺编辑者、卷号和页码；
  - [37] AAAI 条目缺 volume/issue/pages；
  - [39] Sensors 条目缺 article number/pages；
  - 其他 online-first/2026 条目也应补 DOI 或最终出版信息（如已出版）。
- **为什么必须改**：IEEE 参考文献应足以唯一定位文献，当前格式不完整且不统一。
- **建议改成**：用 DOI/Crossref/出版方页面逐条核对作者、题名、venue、年份、卷期、页码/article number 和 DOI，并统一 IEEE 样式。

#### B37. [35] 与本文对其方法类别的描述需一致

- **原文题名**：`Illumination degradation image restoration with retinex-based latent diffusion model`。
- **问题**：正文把它列为 reflection-removal comparison，且表中标为偏振输入。
- **建议改成**：除完成 A15 的协议说明外，在 related work/experiment 中明确它是“adapted generic restoration baseline”，不要把它直接称为原生 polarized reflection-removal method。

## C. 全文统一替换清单

以下词在作前置复合形容词时应统一加连字符：

| 当前写法 | 建议写法 |
|---|---|
| high reflection | high-reflection |
| low frequency | low-frequency |
| high frequency | high-frequency |
| large scale | large-scale |
| branch specific | branch-specific |
| direction aware | direction-aware |
| polarization derived | polarization-derived |
| geometry related | geometry-related |
| reflection related / reflection sensitive | reflection-related / reflection-sensitive |
| material aware / material related | material-aware / material-related |
| frequency domain（作形容词） | frequency-domain |

同时统一以下符号和名称：

- 总强度：只用 `S0` 或只用 `I`，推荐按 Stokes 定义使用 `S0`；
- 数据集：`PHR Dataset`、`Polarrr Dataset`、`PolarFree Dataset`；
- 模块：PWFFM、PCTI、PSRM 的全称、大小写和首次定义位置；
- 偏振角：`AoLP ∈ [0,π)`，所有训练和评价均按 π 周期处理；
- 图中 `GT`、`Transmission`、`Reference` 的含义必须在每个图注中明确。

## 建议修改顺序

1. 先修正式 (6)、AoLP loss/metric/mask（A1、A2、A18、A19）。
2. 补全模型定义、PSRM、训练损失和五通道物理一致性（A2–A7）。
3. 补全数据采集、划分及跨数据集协议（A8–A13）。
4. 重核基线、表 I/II、公平性和统计显著性（A14–A21）。
5. 收紧第 V 章应用结论（A22–A25）。
6. 最后统一语言、图注、术语和参考文献（B1–B37、C）。
